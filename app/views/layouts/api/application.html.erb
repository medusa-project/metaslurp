<!DOCTYPE html>
<html>
<head>
  <title>HTTP API | <%= Option::string(Option::Keys::WEBSITE_NAME) %></title>

  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag 'api', media: 'all' %>

</head>

<body <%= !yield(:body_id).empty? ? raw("id=\"#{yield(:body_id)}\"") : nil %>
      <%= raw("data-nav=\"#{yield(:active_nav)}\"") %>>

  <div class="container-fluid">
    <%= yield.force_encoding(Encoding::UTF_8) %>
  </div>

</body>
</html>
