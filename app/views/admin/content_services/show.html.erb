<% provide :title, @content_service %>
<% provide :active_nav, 'entities' %>

<ol class="breadcrumb">
  <li><%= link_to 'Home', admin_root_path %></li>
  <li><%= link_to 'Content Services', admin_content_services_path %></li>
  <li class="active"><%= @content_service.name %></li>
</ol>

<% if current_user.admin? %>
  <%= button_bar({ label: 'Reindex',
                   icon: 'fa-refresh',
                   method: 'post',
                   url: admin_content_service_reindex_path(@content_service) },
                 { label: 'Edit',
                   icon: 'fa-pencil',
                   url: edit_admin_content_service_path(@content_service) },
                 { label: 'Delete',
                   icon: 'fa-trash',
                   url: admin_content_service_path(@content_service),
                   method: 'delete',
                   class: 'btn-danger',
                   confirm: 'Are you sure you want to delete this content service?' }) %>
<% end %>

<h1><%= @content_service.name %></h1>

<dl>
  <dt>URI</dt>
  <dd><%= @content_service.uri.present? ?
              link_to(@content_service.uri, @content_service.uri) : 'None' %></dd>
  <dt>Key</dt>
  <dd><code><%= @content_service.key %></code></dd>
  <dt>Description</dt>
  <dd><%= @content_service.description.present? ?
              @content_service.description : 'None' %></dd>
</dl>
